while True:
    num = input("Enter your number ")
    if num.isdigit():
        print ("The number your've entered has", len(num), "digits")
        break
    else:
        print ("Your number must not contain a letter")
#  ham dem ki tu: len(a)
# Viết một chương trình yêu cầu người dùng nhập vào một số, đếm số chữ số của số vừa được nhập vào

